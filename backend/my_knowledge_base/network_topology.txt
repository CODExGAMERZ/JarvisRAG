Security Network topology documentation.

The backend administration node for the sandbox runs exclusively on subnet 10.144.9.0/24. The database server sits at IP address 10.144.9.89 and operates on non-standard port 9876. All incoming requests outside this range are silently blackholed at the router.